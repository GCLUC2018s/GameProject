#include "CEnemy.h"

CEnemy::CEnemy(CVector2D pos){
	m_img = *dynamic_cast<CAnimImage*>(GET_RESOURCE("Enemy"));
	m_img.SetRect(0, 64, 64, 128);
	m_img.SetSize(213, 256);
	m_pos = pos;
}

void CEnemy::Update(){

	m_pos.y += 5;
	//if (m_pos.x < 0) {
	//	m_flipH = true;
	//}

	//if (m_pos.x > 1280 - 213) {
	//	m_flipH = false;
	//}

	//if (m_flipH) {
	//	m_pos.x += 5;
	//}
	//else {
	//	m_pos.x -= 5;
	//}
}

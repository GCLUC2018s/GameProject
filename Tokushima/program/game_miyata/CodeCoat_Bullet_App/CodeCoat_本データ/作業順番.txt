四画面(タイトルなど)の表示
	↓
game.screenのhとcppの修正
	&
mapのhとcppの作成 ←定数
		　　CMapControl{
			m_mapdata[定数][定数] 無駄をなくしたいならVectorを使う
		       	m_img[定数]
			loadMap()
			loadFiles()
			draw()
		     }

	↓
playerとCharaData.hの作成
	CCharaData{
	  CVector2D pos;
	  TURN turn;
	  animcounter;
	}

player定数4
	m_img[];
	loadFiles();
	init();
	move();
	draw();